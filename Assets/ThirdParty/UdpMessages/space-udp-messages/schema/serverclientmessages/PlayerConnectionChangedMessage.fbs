namespace UdpMessages.ServerClientMessages;

struct PlayerConnectionChangedMessageFix {
  playerNumber: uint8;
  isConnected:  bool;
}

table PlayerConnectionChangedMessage {
  fix: PlayerConnectionChangedMessageFix (required);
}
